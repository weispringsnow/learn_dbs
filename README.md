###建一个远程库

```shell
echo "# learn_dbs" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/weispringsnow/learn_dbs.git
git push -u origin master
把origin设为默认主机
```

